# 2026-09-11 工作日志

## 收尾：②③ 全部补齐 + 上线尝试

### 无障碍（axe-core，17 页 × 桌面/移动 = 34 次）
- 修复项：统计页文件输入缺 label（critical）、猫咪机与主题聊天的 `aria-label` 用在无 role 的 span/div 上（改 `role="status"` / `role="progressbar"`）、统计页饼图扇区是 recharts 生成的 `role="img"` 无名 path ×10（有完整文本图例 → 图表整体标 `aria-hidden`）、`/miia/world` 可横向滚动 JSON 区不可聚焦（加 `role="group"` + `tabIndex=0`）、/codex 页内嵌 `<main>` 与全站 main 冲突（改 div）、标题层级（角色/故事/API 三页补 sr-only h2，h4 统一 h3）、首页分隔符对比度 3.8:1 → 4.5:1 以上。
- **终态：0 个 critical/serious/moderate 违规。**

### 移动端触控（390px 触摸模拟，24 个页面）
- 用「中心 ±20px 真实命中测试」判定热区，避免被 `getBoundingClientRect` 误导；伪元素扩展热区会被相邻兄弟元素挡住 → 改为**真实** `min-height: 44px`（`.tap-safe`、`.aurora-footer-social`、`.fsiii-character-link`、`.nctb-page-footer a`）。
- 口径：硬性阈值取 WCAG 2.5.8 AA（24×24），44px 作为建议项单列；段落内联链接与棋盘内游戏目标按 2.5.8 豁免并注明理由。
- 结果：**硬性不达标 0**（仅剩 24-43px 建议项 + 2 处已注明的豁免）。零横向溢出。
- NeuralEcho 追加「邻近点按」——点棋盘空白处选最近的己方生长点（等效热区约 80px），并放大圆点。

### 性能实测（真实 LCP/CLS，Edge + 4G 限速）
- **CLS 修复是这轮最大收益**：`/characters/miia` 1.015 → 0.050，首页 4G 1.000 → 0.080，猫咪机 0.601 → 0.156。
- 根因是「加载骨架 512px → 页面内容 900px」的整体替换把页脚推出视口：给 `.aurora-site-main` 与 `.aurora-state--page` 加一屏最小高度即解决。
- 猫咪机装饰光斑 `absolute` 锚在会变高的容器上 → 改 `fixed`（贡献 0.44 的偏移消失）。
- 反例记录：把引导弹窗的位移入场改成纯淡入反而让 CLS 从 0.157 升到 0.520（位移入场在"遮挡"这次偏移），已回退。
- 桌面指标：FCP 200-450ms、LCP 376-1528ms、TTFB 8-19ms、长任务 0。

### 另外两件事
- **AI 对话端到端**：浏览器实跑一轮，8 → 16 消息节点，无 key 时走 `local-grounded-preview` 兜底（明确告知未接模型），零 JS 错误。
- **游戏分享卡**：新增 `npm run og:generate`（playwright-core + 系统 Edge 渲染 1200×630 卡片，15 个游戏），`RouteMetadata` 用前缀规则接入；猫咪机保留原插画卡。

### 回归中抓到的真 Bug
- `/chat` 页 404：`ChatSelect.css` 用 CSS 背景引用了**会被 dist 裁剪的原图**（有优化变体的原图会被移除）。修为 `image-set` 优化变体；并全站扫描同类引用，顺带把首页 hero 的 jpeg 兜底也换成变体。
- 顺带做 image-set 回退：不支持 image-set 的浏览器（Firefox <113）此前会丢掉整条 background-image，现在先写普通声明兜底。

## 未完成 / 受阻
- **发布上线失败**：`workbuddy_sites_deploy` 连续两次返回 `fetch failed`（发布服务网络不可达，疑似本机代理环境所致），未拿到分享链接。已备好发布链路（`scripts/serve-dist.mjs` 零依赖静态服务 + `npm start` + vite `allowedHosts`），网络恢复即可发。

## 经验
- 触摸热区：伪元素扩展不可靠（会被后绘制的兄弟元素拦截）；要真正达标就加 `min-height`，内联文本链按 WCAG 2.5.8 豁免。
- CLS 最容易被忽略的来源：加载骨架与真实内容的高度差。
- 任何 CSS/TS 里引用 `public/**` 原始图片前，先对照 `imageVariants.json`——有变体的原图会被 prepare-sites 从 dist 删掉。

## 发布复核（15:30 之后）
- 重试 `workbuddy_sites_deploy` 第 3 次仍为 `fetch failed`（发布服务网络不可达，非代码问题）。
- 已再次确认：`npm run build` 通过、全站回归 38/38 通过（19 页 × 桌面/移动，0 JS 错误 / 0 404 / 0 横向溢出）、CLS 0.050–0.156。
- 发布前置条件都已就绪：`scripts/serve-dist.mjs`（零依赖、SPA 回退、dist 缺失自动构建）、`npm start`、vite `server/preview.allowedHosts = true` 与 `host 0.0.0.0`。
- 网络恢复后可直接重跑发布，命令与参数见对话。

## 新增技能
- `~/.workbuddy/skills/web-quality-audit/SKILL.md`：axe 无障碍 + CLS 实测 + 移动触摸目标 + 全站回归 的判定口径/脚本/修法（含本次全部真因与反例）。

## 审计脚本位置（在项目外，可复用）
- `C:\Users\Administrator\.workbuddy\binaries\node\workspace\`：`audit-a11y.js`、`audit-mobile.js`、`audit-perf.js`、`audit-chat.js`、`audit-final.js`、`probe-cls.js`、`probe-svg-datad.js`、`audit-a11y-detail.js`

## 修复部署工具「pnpm 运行时不存在」（16:00）
- 症状：桌面 `2-部署网页.cmd` → `scripts/desktop-site-tool.mjs deploy` 报“失败：pnpm 运行时不存在：C:/Users/Administrator/.cache/codex-runtimes/.../pnpm/bin/pnpm.mjs”。
- 根因：`PNPM_BIN` 是**写死的绝对路径**，指向 codex 运行时自带的 pnpm；该目录在本机不存在（机器上也没装 pnpm）。原先只有 `requireFile(PNPM_BIN)` 一条路，必然失败。
- 修复：改为三级 runner 解析 `describeRunner()`——**本地 wrangler → pnpm dlx → npx --yes**；新增 `runViaShell()`（Windows 上 .cmd 必须经 cmd.exe）；`--check` 分支不再强制要求 pnpm/wrangler，并打印实际采用的部署方式。
- 同时 `npm install -D wrangler`（4.131.0），部署时零下载、不依赖 pnpm。
- 已验证：`node scripts/desktop-site-tool.mjs deploy --check` → “部署工具检查通过，目标：nyaumae.pages.dev / 部署方式：本地 wrangler”；`wrangler pages deploy` 支持 `--project-name/--branch`；`node --check` 语法 OK。**未实际执行部署**（会覆盖线上内容，等用户确认）。

## 重要事实澄清
- 用户的线上站点是 **https://nyaumae.pages.dev（Cloudflare Pages，返回 200，正常）**。
- `zhi-yi-dialogue-os.nyaumae.chatgpt.site` 返回 403 是**另一个已失效域名**，与线上站点无关（此前误判为"线上 403"，已更正）。
- 用户侧的部署链路：桌面 `1-构建网页.cmd`（build）→ `2-部署网页.cmd`（deploy to Cloudflare Pages），Token/项目名从项目根的 `启动部署工具.bat` 读取。

## 桌面三个脚本彻底清理（16:20）
- 三个 .cmd（1-构建/2-部署/3-预览）原先把 `TOOL_NODE` 写死到已失效的 codex 运行时 node，只靠 `if not exist ... set TOOL_NODE=node` 隐式兜底。
- 现改为：显式 `set "TOOL_NODE=node"` + `where node` 检查 + 找不到时中文提示并退出；死路径行已删除（3 个文件 0 引用）。
- **踩坑**：用 Python 的 io.open 文本模式重写 .cmd，会把 CRLF 读成 LF 再原样写出 → 文件变 LF-only；批处理含 `goto`/标签，Windows 下应保持 CRLF。已用二进制方式统一回 CRLF（以后改 .cmd 必须注意）。
- 负载实测：`node scripts/desktop-site-tool.mjs build` 用系统 node 跑通（✓ built、构建成功未部署）；`deploy --check` 通过（目标 nyaumae.pages.dev，方式=本地 wrangler）；`preview` 用的 vite preview 本日多次跑通。
- 限制：本环境禁止从 shell 调 cmd.exe，无法真正双击式运行 .cmd 外壳，只做了"负载等价验证 + 语法核对"，脚本外壳需用户双击确认一次。
- 桌面 `sev.bat` / `录屏.bat` 与本项目无关，未改动。
