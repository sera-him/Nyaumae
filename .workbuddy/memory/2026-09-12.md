# 2026-09-12 工作日志

## 复查：搜索错字修复 + 新词频（23:15）
- 确认上一轮修复已全部生效：线上 `nyaumae.pages.dev/assets/fullSearchIndex-CS3pZIJT.js` 与本地 dist 逐字节一致；运行时 `fullTextSearch('咪呀的愿望')` 首条命中 `[故事] 咪呀的愿望`（2650 分），第二条 `[页面] 咪呀空间`，"米娅*"变体已彻底消失。
- **站点真实词频**（口径：`src/data/wordFrequency.ts`，Intl.Segmenter 分词，大人国正文 0.1 加权）：
  773 条目 / 总词数 24,260 / 唯一词 9,594 / 角色词 44 个。
  Top10：一个 156.4、她的 137.5、没有 118、**咪呀 117.3**、小满 110、不是 108.8、自己 100.8、规则 90.4、然后 86、土地 81。
  角色词：咪呀 117.3 > 小满 110 > 喵呜 59 > 米雅 38 > mia³ 32 = 米娅 32 > Mia 19 > Damocles 7。
- **重要口径差异**：`scripts/wordfreq_snapshot.py` 是近似统计（扫源码文本 + 连续汉字整段提取），会混入 className/div/const 等代码标识符，且"咪呀的愿望"被当成整词（count 3）；站点真实分词会切成 咪呀/的/愿望，所以该短语在真实口径下**没有独立计数**（"愿望"=8）。以后报告词频应以运行时口径为准。
- 统计方法（可复用）：用 `node_modules/.bin/esbuild tmp/freq-report.ts --bundle --platform=node --format=esm --alias:@=./src --outfile=tmp/freq-report.mjs` 打包后 `node` 运行，直接调 `loadWordFrequency()` 拿真实数据；产物 `tmp/wordfreq-real.txt`。
