# Aurora 全站 UI 迁移计划

## 目标

将主页已经采用的 Aurora Atlas 视觉语言逐步应用到全站，同时遵守以下原则：

- 不删除现有信息、入口或功能。
- 不改写游戏、公式、搜索、音乐等业务逻辑，视觉层与逻辑层分离。
- 所有页面支持桌面、平板与手机。
- 长文本和复杂交互页面优先保证可读性，不强行使用高密度粒子或强光效果。
- 每批迁移后执行生产构建、页面视觉检查、交互检查和控制台错误检查。

## 视觉方向

- 基础背景：深紫黑宇宙背景。
- 强调色：青、紫、粉、金、蓝。
- 标题、数字和重要关键词可使用彩色渐变。
- 正文保持高对比度浅色，避免大段渐变文字。
- 玻璃面板承载信息，彩色描边表达页面类型和状态。
- 粒子作为共享氛围层，根据页面密度和设备性能调整。

## 页面类型与主色

| 页面类型 | 主色 |
| --- | --- |
| 世界观 | 青色 |
| 角色 | 紫色 |
| 故事 | 粉色 |
| 咪呀空间 | 粉紫色 |
| 数学与 FSIII | 金色 |
| 游戏 | 蓝色或动态色 |
| 设置与工具 | 中性紫灰 |

## 实施阶段

### 1. 建立全站设计系统

- [x] 建立 Aurora 颜色、背景、间距、圆角、阴影和动效令牌。
- [x] 建立页面骨架、玻璃面板、渐变标题、按钮、标签、统计块等基础样式。
- [x] 建立可复用 React UI 原语。
- [x] 提供键盘焦点和减少动态效果支持。

### 2. 改造全站框架

- [x] 统一顶部导航、搜索和音乐按钮视觉；重构桌面快捷入口与移动端全量菜单。
- [x] 建立共享页面 Aurora 背景氛围层（具体粒子仍由页面按需管理）。
- [x] 建立路由切换的轻量过渡。

### 3. 按页面类型迁移

- [x] 世界观页（概览、QET、编年史、组织机构、世界观详述的统一外层与导航）。
- [x] 角色列表页（档案筛选、主角色、补充角色与关系网络）。
- [x] 设置页。
- [x] 角色详情页。
- [x] 故事列表与阅读器。
- [x] 咪呀空间与数学模型。
- [x] Playground 与各个游戏。
- [x] API 文档及其他工具页。

### 4. 全站验证

- [x] 核对文本、数据和入口完整性；补接 API 路由并修正旧 FSIII 路径。
- [x] 检查搜索、筛选、标签页、弹窗和游戏入口交互。
- [x] 检查图片、公式、图表与媒体容器。
- [x] 检查桌面与手机布局（页面无横向溢出）。
- [x] 检查 Canvas 容器、生产构建结果和控制台错误。

## 当前进度

- 主页：已正式采用 Aurora Atlas UI。
- 粒子：主页支持指针连线与点击无限新增。
- 隔离原型：保留在 `public/examples/home-ui/`。
- 设计系统：第一阶段已完成，位于 `src/styles/aurora-design-system.css` 和 `src/components/aurora/`。

## 当前状态

所有一级页面、内容页与工具页均已接入 Aurora Atlas 视觉体系。进入全站验证与后续细节维护阶段。

## 网站 AI 迁移记录（2026-07-27）

- 对话工作台、AI 设置和 API 参考页沿用 Aurora Atlas 页面容器与状态样式。
- 网站 AI 入口统一为 `/chat`，AI 配置统一为 `/settings/ai`，API 参考统一为 `/api`。
- 浏览器本地状态键为 `nyaumae:conversation-state:v1` 与 `nyaumae:ai-config:v1`；后续结构变化必须通过版本迁移处理。
- BYOK 密钥只在当前浏览器本地使用和保存，页面只显示掩码；导出、日志和错误信息不包含完整密钥。
- 流式输出使用 Fetch ReadableStream / SSE；本阶段不新增 WebSocket。
- 网站知识引用必须带标题、来源类型、相关 ID、摘要和已验证站内路由；找不到可靠资料时不生成虚假来源。
- 当前阶段只交付网站 AI，不把 NCTB、故事、图片和游戏标记为已完成。
